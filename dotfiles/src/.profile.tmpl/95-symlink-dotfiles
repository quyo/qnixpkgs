

[ -e "$HOME/.joe" ]             || ln -sf "$QDOTFILES/.joe"             "$HOME/.joe"
[ -e "$HOME/.direnvrc" ]        || ln -sf "$QDOTFILES/.direnvrc"        "$HOME/.direnvrc"
[ -e "$HOME/.gitconfig" ]       || ln -sf "$QDOTFILES/.gitconfig"       "$HOME/.gitconfig"
[ -e "$HOME/.joerc" ]           || ln -sf "$QDOTFILES/.joerc"           "$HOME/.joerc"
[ -e "$HOME/.screenrc" ]        || ln -sf "$QDOTFILES/.screenrc"        "$HOME/.screenrc"
[ -e "$HOME/.selected_editor" ] || ln -sf "$QDOTFILES/.selected_editor" "$HOME/.selected_editor"

mkdir -p "$HOME/.config/croc"
[ -e "$HOME/.config/croc/receive.json" ] || ln -sf "$QDOTFILES/.config/croc/receive.json" "$HOME/.config/croc/receive.json"
[ -e "$HOME/.config/croc/send.json" ]    || ln -sf "$QDOTFILES/.config/croc/send.json"    "$HOME/.config/croc/send.json"

[ -e "$HOME/.config/starship.toml" ]     || ln -sf "$QDOTFILES/.config/starship.toml"     "$HOME/.config/starship.toml"
