

if [[ $- =~ .*i.* ]] && [ -x "$(command -v procs)" ]; then

  if [ ! -e "$HOME/.config/procs/config.toml" ]; then
    mkdir -p "$HOME/.config/procs"
    ln -sf "$QDOTFILES/.config/procs/config.toml" "$HOME/.config/procs/config.toml"
  fi

  source <(procs --gen-completion-out bash)

fi
